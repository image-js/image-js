{
  "name": "image-js",
  "version": "0.1.6",
  "description": "Image processing and manipulation in JavaScript",
  "keywords": [
    "image",
    "processing",
    "manipulation",
    "analysis",
    "histogram",
    "hsb",
    "hsl",
    "rgb",
    "roi",
    "rois",
    "mask"
  ],
  "author": "Michaël Zasso",
  "contributors": [
    "Luc Patiny"
  ],
  "repository": "image-js/core",
  "bugs": {
    "url": "https://github.com/image-js/core/issues"
  },
  "homepage": "https://github.com/image-js/core",
  "license": "MIT",
  "main": "./lib/index.js",
  "scripts": {
    "build-dist": "browserify src/index.js -o dist/image.js --standalone IJS --exclude canvas --ignore buffer --ignore zlib --ignore fs --global-transform [ babelify ]",
    "build": "npm run compile && npm run clean-dist && npm run build-dist",
    "build-test": "browserify test/browser/bootstrap.js -o test/browser/image.js --standalone IJS --exclude canvas --ignore buffer --ignore zlib --ignore fs --debug --global-transform [ babelify --sourceMap ]",
    "compile": "npm run clean-lib && babel src --out-dir lib",
    "clean-dist": "rm -rf dist && mkdir dist",
    "clean-lib": "rm -rf lib",
    "eslint": "eslint src test/node",
    "eslint-fix": "eslint src test/node --fix",
    "prepublish": "npm run compile",
    "test": "sh test/checkOnly.sh && npm run test-mocha && npm run eslint",
    "test-cov": "node -r ./test/node/babel node_modules/.bin/babel-istanbul cover _mocha -- --require should --reporter dot --recursive test/node",
    "test-mocha": "node -r ./test/node/babel node_modules/.bin/_mocha --require should --reporter mocha-better-spec-reporter --recursive test/node",
    "test-travis": "sh test/checkOnly.sh && node -r ./test/node/babel node_modules/.bin/babel-istanbul cover _mocha --report lcovonly -- --require should --reporter mocha-better-spec-reporter --recursive test/node && npm run eslint",
    "watch-browser": "watchify test/browser/bootstrap.js -o test/browser/image.js --standalone IJS --exclude canvas --ignore buffer --ignore zlib --ignore fs --debug --global-transform [ babelify --sourceMap ]",
    "watch-node": "babel --watch src --out-dir lib"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-istanbul": "0.4.0",
    "babel-plugin-transform-es2015-block-scoping": "^6.1.18",
    "babel-preset-es2015-node4": "^2.0.1",
    "babel-register": "^6.2.0",
    "babelify": "^7.2.0",
    "benchmark": "^1.0.0",
    "browserify": "^12.0.1",
    "eslint": "^1.10.1",
    "mocha": "^2.3.4",
    "mocha-better-spec-reporter": "^3.0.1",
    "rimraf": "^2.4.0",
    "sha.js": "^2.4.3",
    "should": "^7.0.1",
    "watchify": "^3.6.0",
    "which": "^1.2.0"
  },
  "optionalDependencies": {
    "canvas": "^1.3.1"
  },
  "dependencies": {
    "array-includes": "^3.0.1",
    "atob-lite": "^1.0.0",
    "extend": "^3.0.0",
    "fast-png": "0.0.4",
    "is-array-type": "^1.0.0",
    "is-integer": "^1.0.6",
    "ml-matrix": "^1.0.3",
    "ml-regression": "^1.1.1",
    "new-array": "^1.0.0",
    "string-includes": "^1.0.0",
    "tiff": "0.0.2",
    "web-worker-manager": "^0.2.0"
  },
  "engines": {
    "node": ">= 4.0.0"
  }
}
