{
  "name": "image-js-root",
  "private": true,
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "lerna": "^3.13.1",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.4000"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "jest": "jest",
    "prepare": "npm run bootstrap",
    "test": "npm run jest"
  }
}
