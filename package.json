{
  "name": "image-js",
  "version": "0.0.3",
  "description": "Image processing and manipulation in JavaScript",
  "keywords": [
    "image",
    "processing",
    "manipulation",
    "analysis",
    "histogram",
    "hsb",
    "hsl",
    "rgb",
    "roi",
    "rois",
    "mask"
  ],
  "author": "Michaël Zasso",
  "contributors": [
    "Luc Patiny"
  ],
  "repository": "image-js/core",
  "bugs": {
    "url": "https://github.com/image-js/core/issues"
  },
  "homepage": "https://github.com/image-js/core",
  "license": "MIT",
  "main": "./lib/image.js",
  "scripts": {
    "build": "npm run clean-dist && browserify src/image.js -o dist/image.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --transform babelify",
    "build-test": "browserify test/browser/bootstrap.js -o test/browser/image.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --debug --transform [ babelify --sourceMap ]",
    "compile": "npm run clean-lib && babel src --out-dir lib",
    "clean-dist": "rm -rf dist && mkdir dist",
    "clean-lib": "rm -rf lib",
    "eslint": "eslint src test/node",
    "karma": "karma start",
    "prepublish": "npm run compile",
    "test": "npm run compile && npm run test-mocha && npm run eslint",
    "test-mocha": "mocha --compilers js:babel/register --require should --reporter mocha-better-spec-reporter --recursive test/node",
    "watch-browser": "watchify test/browser/bootstrap.js -o test/browser/image.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --debug --transform [ babelify --sourceMap ]",
    "watch-node": "babel --watch src --out-dir lib"
  },
  "devDependencies": {
    "babel": "^5.6.4",
    "babelify": "^6.1.2",
    "benchmark": "^1.0.0",
    "browserify": "^10.2.4",
    "eslint": "^1.0.0-rc-1",
    "ij-test": "^1.5.0",
    "karma": "^0.13.2",
    "karma-babel-preprocessor": "^5.2.1",
    "karma-chrome-launcher": "^0.2.0",
    "karma-firefox-launcher": "^0.1.6",
    "karma-mocha": "^0.2.0",
    "karma-safari-launcher": "^0.1.1",
    "mocha": "^2.2.5",
    "mocha-better-spec-reporter": "^2.1.1",
    "rimraf": "^2.4.0",
    "should": "^7.0.1",
    "watchify": "^3.2.3"
  },
  "optionalDependencies": {
    "canvas": "^1.2.3"
  },
  "dependencies": {}
}
