{
  "name": "image-js",
  "version": "0.0.1",
  "description": "Image processing and manipulation in JavaScript",
  "keywords": [
    "image",
    "processing",
    "manipulation",
    "analysis",
    "histogram",
    "hsb",
    "hsl",
    "rgb",
    "roi",
    "rois",
    "mask"
  ],
  "author": "Michaël Zasso",
  "contributors": [
    "Luc Patiny"
  ],
  "repository": "image-js/ij",
  "bugs": {
    "url": "https://github.com/image-js/ij/issues"
  },
  "homepage": "https://github.com/image-js/ij",
  "license": "MIT",
  "main": "./lib/ij.js",
  "files": [
    "./dist",
    "./lib"
  ],
  "scripts": {
    "build": "npm run clean-dist && browserify src/ij.js -o dist/ij.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --transform babelify",
    "build-test": "browserify test/browser/bootstrap.js -o test/browser/ij.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --transform babelify",
    "compile": "npm run clean-lib && babel src --out-dir lib",
    "clean-dist": "rm -rf dist && mkdir dist",
    "clean-lib": "rm -rf lib",
    "prepublish": "npm run compile",
    "test": "npm run compile && npm run test-mocha",
    "test-mocha": "mocha --compilers js:babel/register --require should --reporter mocha-better-spec-reporter --recursive test/node",
    "watch-browser": "watchify test/browser/bootstrap.js -o test/browser/ij.js --standalone IJ --exclude canvas --exclude canvas/lib/bindings --ignore fs --transform babelify",
    "watch-node": "babel --watch src --out-dir lib"
  },
  "devDependencies": {
    "babel": "^5.6.4",
    "babelify": "^6.1.2",
    "benchmark": "^1.0.0",
    "browserify": "^10.2.4",
    "ij-test": "^1.4.0",
    "mocha": "^2.2.5",
    "mocha-better-spec-reporter": "^2.1.1",
    "rimraf": "^2.4.0",
    "should": "^7.0.1",
    "watchify": "^3.2.3"
  },
  "optionalDependencies": {
    "canvas": "^1.2.3"
  },
  "dependencies": {
  }
}
